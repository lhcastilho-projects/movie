<?xml version="1.0" encoding="UTF-8"?>
<server description="movie">

    <!-- Enable features -->
    <featureManager>
        <feature>webProfile-10.0</feature>
    </featureManager>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080"
                  httpsPort="-1" />

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <!-- Configures the application on a specified context root -->
    <webApplication contextRoot="/movie" location="movie.war" />

    <!-- Default SSL configuration enables trust for default certificates from the Java runtime -->
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />

    <!-- SQLite Library configuration -->
    <library id="SQLiteLib">
        <fileset dir="${server.config.dir}/lib/global" includes="*.jar" />
    </library>

    <!-- Database Configuration -->
    <dataSource id="movie" jndiName="jdbc/movie" type="javax.sql.DataSource" transactional="true" >
        <jdbcDriver libraryRef="SQLiteLib"
                    javax.sql.ConnectionPoolDataSource="org.sqlite.javax.SQLiteConnectionPoolDataSource" 
                    javax.sql.DataSource="org.sqlite.SQLiteDataSource" />
        <properties URL="jdbc:sqlite:${wlp.install.dir}../../../db/movies.db" />
    </dataSource>

</server>
